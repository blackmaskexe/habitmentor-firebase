{
  "name": "habitmentor-firebase-functions",
  "version": "1.0.0",
  "description": "backend for habitmentor-ai, an AI powerer habit tracking application",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "echo \"no linting\"",
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "author": "blackmaskexe",
  "license": "ISC",
  "dependencies": {
    "@expo/vector-icons": "^14.1.0",
    "express": "^5.1.0",
    "firebase": "^12.1.0",
    "firebase-admin": "^13.4.0",
    "firebase-functions": "^6.4.0",
    "openai": "^5.12.1"
  },
  "devDependencies": {
    "globals": "^16.3.0",
    "typescript": "^5.9.2"
  }
}
